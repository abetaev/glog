{
	"//": "-unfortunately npm is too \"smart\" to keep more than one comment per section-",
	"name": "@tlog/commen",
	"version": "1.1.1",
	"description": "comment log, of course",
	"keywords": [
		"tlog",
		"commen"
	],
	"license": "MIT",
	"scripts": {
		"//": "... if we in \"package.json\" we expect `node` to be...",
		"start": "node start.js"
	},
	"dependencies": {
		"opn": "^5.4.0"
	},
	"devDependencies": {
		"//": "UX RULE: zero-configuration",
		"parcel": "^1.12.1"
	}
}
